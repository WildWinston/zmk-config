# GitHub Actions build matrix
# Pinned to ZMK v0.3 — use classic board IDs (not //zmk variants).
---
include:
  - board: nice_nano_v2
    shield: vanana_rev2

  - board: nice_nano
    shield: waaffle_rev3

  - board: nice_nano_v2
    shield: waaffle_rev3
